<Window x:Class="AdminControl.WPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Авторизація - Admin Control" Height="500" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#ECF0F1">

    <Window.Resources>
        <Style x:Key="ValidationTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <StackPanel>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3,3,0,0">
                                <ScrollViewer x:Name="PART_ContentHost" 
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                            </Border>
                            <TextBlock x:Name="ErrorMessage"
                                       Text="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource TemplatedParent}}"
                                       Foreground="#E74C3C"
                                       FontSize="11"
                                       Margin="2,4,0,0"
                                       Visibility="Collapsed"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter Property="BorderBrush" Value="#E74C3C"/>
                                <Setter TargetName="ErrorMessage" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#3498DB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <AdornedElementPlaceholder/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ValidationPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="0,0,0,2"/>
            <Setter Property="Background" Value="#F8F9FA"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#3498DB"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="White" CornerRadius="10" Margin="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>
        
        <Grid Margin="30">
            <StackPanel VerticalAlignment="Center">

                <!-- іконка -->
                <TextBlock Text="🔐" 
                           FontSize="48" 
                           HorizontalAlignment="Center"
                           Margin="0,0,0,10"/>

                <TextBlock Text="Admin Control" 
                           FontSize="28" FontWeight="Bold" 
                           Foreground="#2C3E50"
                           HorizontalAlignment="Center"/>
                
                <TextBlock Text="Вхід у систему управління" 
                           FontSize="13" 
                           Foreground="#7F8C8D"
                           HorizontalAlignment="Center" 
                           Margin="0,5,0,30"/>

                <!--поле логіну  -->
                <TextBlock Text="Логін" Foreground="#7F8C8D" FontSize="12" Margin="0,0,0,5"/>
                <TextBox Style="{StaticResource ValidationTextBoxStyle}"
                         Text="{Binding Login, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}" 
                         Height="40" 
                         Margin="0,0,0,15"/>

                <!-- поле паролю -->
                <TextBlock Text="Пароль" Foreground="#7F8C8D" FontSize="12" Margin="0,0,0,5"/>
                <PasswordBox x:Name="pbPassword" 
                             Style="{StaticResource ValidationPasswordBoxStyle}"
                             Height="40" 
                             Margin="0,0,0,5"/>

                <!-- повідомлення про помилку паролю -->
                <TextBlock Text="{Binding ErrorMessage}" 
                           Foreground="#E74C3C" 
                           TextWrapping="Wrap" 
                           HorizontalAlignment="Left" 
                           Margin="2,0,0,15"
                           FontSize="11"
                           Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>

                <!-- кнопка входу з командою -->
                <Button Content="Увійти" 
                        Command="{Binding LoginCommand}" 
                        CommandParameter="{Binding ElementName=pbPassword}"
                        Height="45" 
                        FontSize="16" 
                        FontWeight="Bold"
                        Background="#2980B9" 
                        Foreground="White" 
                        BorderThickness="0" 
                        Cursor="Hand"
                        Margin="0,10,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1F618D"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#BDC3C7"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

            </StackPanel>

            <!-- індикатор завантаження -->
            <Grid Background="#CCFFFFFF" 
                  Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="⏳ Перевірка..." 
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center" 
                               FontWeight="Bold"
                               FontSize="14"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
