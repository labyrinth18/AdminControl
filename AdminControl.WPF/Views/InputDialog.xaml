<Window x:Class="AdminControl.WPF.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="180" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#ECF0F1">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="{Binding Prompt}" 
                   FontSize="14" 
                   Foreground="#2C3E50"
                   Margin="0,0,0,10"/>

        <TextBox Grid.Row="1" 
                 x:Name="txtResponse"
                 Text="{Binding ResponseText, UpdateSourceTrigger=PropertyChanged}"
                 Height="35" 
                 FontSize="14"
                 Padding="8"/>

        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="Скасувати" 
                    Width="100" 
                    Height="35"
                    Click="Cancel_Click"/>
            <Button Content="OK" 
                    Width="100" 
                    Height="35"
                    Background="#27AE60"
                    Foreground="White"
                    Click="OK_Click"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
