using AdminControl.DAL; // Додаємо using

namespace AdminControl.DALEF.Concrete
{
    public class AdminActionLogRepository : IAdminActionLogRepository
    {
        // Можна додати конструктор для DbContext
        // private readonly AdminControlContext _context;
        // public AdminActionLogRepository(AdminControlContext context) => _context = context;

        // Поки що клас порожній
    }
}